import React from 'react'

export default (props)=>(

    <div>
    <p>name : {props.follower.nameOf}</p>
    <p>family : {props.follower.family}</p>
    <p>id : {props.follower.id}</p>
    </div>
    
)